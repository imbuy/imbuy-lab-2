spring:
  application:
    name: lot-service-test
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

# Scheduler настройки для тестов
scheduler:
  fixed-rate: 60000